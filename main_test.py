import unittest # Unit test framework (standard library) integrated in Python
from unittest.mock import patch # patch is used to replace parts of the code being tested with mock objects
from io import StringIO # Evaluates inputs/outputs from print and input functions
import sys
